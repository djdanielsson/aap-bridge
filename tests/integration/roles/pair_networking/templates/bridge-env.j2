# Auto-generated by aap-bridge testing infrastructure
# Pair: AAP {{ source_version }} -> AAP {{ target_version }}
# Generated: {{ ansible_date_time.iso8601 }}

AAP_BRIDGE_CONFIG={{ playbook_dir }}/../generated/pairs/{{ pair_id }}/config.yaml
AAP_BRIDGE_LOG_LEVEL=INFO

SOURCE__VERSION="{{ source_version }}"
TARGET__VERSION="{{ target_version }}"

SOURCE__URL=https://localhost:{{ src_controller_port }}{{ src_config.api.controller_path }}
SOURCE__TOKEN="{{ source_token }}"
SOURCE__VERIFY_SSL=false
SOURCE__TIMEOUT=30

{% if tgt_config.components.gateway.enabled | default(false) %}
TARGET__URL=https://localhost:{{ tgt_envoy_port }}{{ tgt_config.api.controller_path }}
{% else %}
TARGET__URL=https://localhost:{{ tgt_controller_port }}{{ tgt_config.api.controller_path }}
{% endif %}
TARGET__TOKEN="{{ target_token }}"
TARGET__VERIFY_SSL=false
TARGET__TIMEOUT=30

MIGRATION_STATE_DB_PATH=postgresql://aap_migration_user:{{ aap_pg_password }}@localhost:5432/aap_migration

DRY_RUN=false
RESUME_ENABLED=true
